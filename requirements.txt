torch
transformers>=5.4.0
accelerate
sentencepiece
protobuf
fastapi
uvicorn[standard]
python-multipart
soundfile
librosa
requests
# Kokoro TTS. On Python 3.14 install with:
#   pip install --no-deps --ignore-requires-python kokoro==0.7.16 misaki==0.7.16
# (both declare <3.13; misaki's spacy-based English G2P is bypassed — tts.py
# routes everything through its espeak G2P)
kokoro==0.7.16
misaki==0.7.16
phonemizer-fork
espeakng_loader
num2words
loguru
regex
# misaki[ja]/[zh] G2P deps (Japanese + Mandarin voices)
fugashi
unidic-lite
jaconv
mojimoji
cn2an
jieba
pypinyin
ordered-set
# SILMA TTS (Arabic/English voice cloning). Install with:
#   pip install --no-deps silma-tts
# (its nemo_text_processing dep needs pynini, unbuildable on py3.14 — tts_silma.py
# stubs it with a pass-through normalizer; torchaudio.load is shimmed via
# soundfile; install torchaudio from the pytorch cpu index)
silma-tts
catt-tashkeel
cached_path
hydra-core
omegaconf
pydub
torchdiffeq
vocos
x_transformers
torchaudio
matplotlib
unidecode
# Hebrew ASR backend (ivrit-ai/whisper-large-v3-turbo-ct2, CTranslate2 format).
# Plain install works on Python 3.14 (cp314 wheels exist for ctranslate2, av,
# onnxruntime).
faster-whisper==1.2.1

